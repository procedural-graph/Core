namespace Windows.Win32.System.Threading;

// This is a workaround to get the compiler to recognize PROCESS_INFORMATION as a type in the generated library import code.

internal partial struct PROCESS_INFORMATION;